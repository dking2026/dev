pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat "sudo docker build -t  web-server ."
            }
       }
        stage('Deploy') {
            steps {
                bat "sudo docker run -itd -p 80:80 web-server"
            }
        }
    }
}
